# Optional. Used only by this repo's own dev/test harness and examples — the SDK
# itself never reads process.env implicitly except as PixabayClient's documented
# constructor fallback (new PixabayClient() with no apiKey). Prefer passing
# apiKey explicitly in consumer code; get a free key at https://pixabay.com/api/docs/.
PIXABAY_API_KEY=
